import { Player, Robot, Robot2, Slime, Torch, Keyboard, Box } from '../../../objects';

export default {
    player: Player,
    robot: Robot,
    robot2: Slime,
    torch: Torch,
    keyA: Keyboard.A,
    box: Box,
}